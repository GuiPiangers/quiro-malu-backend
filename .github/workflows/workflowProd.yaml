name: Continues Integration and Delivery

on: 
  push:
    branches:
      - main


jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v2
        with:
          node-version: 22.x
      - run: npm ci
      - run: npm run build

  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v2
        with:
          node-version: 22.x
      - name: Login DockerHub
        run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}}
      - name: Build docker image
        run: docker build -f Dockerfile.prod -t guipiangers/quiro-backend:1.0 .
      - name: Push image docker
        run: docker push guipiangers/quiro-backend:1.0

    needs: tests

  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Create SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.QUIRO_SSH_KEY }}" > ~/.ssh/quiro-github-access-key.pem
          chmod 600 ~/.ssh/quiro-github-access-key.pem
      - name: Test SSH connection
        run: |
          ssh -i ~/.ssh/quiro-github-access-key.pem ec2-user@<${{ secrets.QUIRO_PUBLIC_IP }}> "echo 'SSH connection successful!'"

      - name: Deploy the applications
        run: |
          ssh -i ~/.ssh/quiro-github-access-key.pem ec2-user@<${{ secrets.QUIRO_PUBLIC_IP }}> << EOF
          cd /quiro-malu-backend
          docker pull guipiangers/quiro-backend:1.0
          docker compose down app
          docker compose -f docker-compose.prod.yml up -d app
          EOF
    needs: build
      