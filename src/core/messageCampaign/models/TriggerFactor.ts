import { AvailableAppEvents } from "../../shared/observers/EventListener"

type triggerConfigTable = {
    createPatient: {delay: number, delayUnit: "minutes" | "hours" | "days"}
}

export function triggerFactor({}:{event: AvailableAppEvents, config: }){

}